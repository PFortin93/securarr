# Self-hosted Renovate on a daily schedule. Needs the RENOVATE_TOKEN
# repo secret: a fine-grained PAT for this repo with Contents and
# Pull requests set to Read/Write.
name: renovate
on:
  schedule:
    - cron: '0 12 * * *'   # daily
  workflow_dispatch:
permissions:
  contents: read
jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - uses: renovatebot/github-action@ea850436a5fe75c0925d583c7a02c60a5865461d # v43.0.20
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
        env:
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          LOG_LEVEL: info
